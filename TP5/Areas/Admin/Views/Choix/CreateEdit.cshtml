@model ChoixCreateEditVM
@{
    string textAction = "Ajouter";
    string formAction = "Create";

    if (Model.Choix.Id > 0)
    {
        textAction = "Modifier";
        formAction = "Edit";
    }

    ViewData["Title"] = textAction + " un choix";
}

<div id="choice-createedit-page">
    <div class="row">
        <div class="offset-md-1 offset-lg-2 col-md-10 col-lg-8">
            <div class="page-title">
                <h2>@textAction un choix</h2>
            </div>

            <div class="form-section">
                <form asp-action="@formAction">
                    <input type="hidden" asp-for="Choix.Id" />

                    <div class="form-horizontal">
                        <hr />
                        <div class="form-group row">
                            <label asp-for="Choix.Description" class="col-form-label col-md-3"></label>
                            <div class="col-md-9">
                                <input asp-for="Choix.Description" class="form-control" />
                                <span asp-validation-for="Choix.Description" class="text-danger"></span>
                            </div>
                        </div>
                        <hr />
                    </div>
                    <div class="text-center">
                        <input type="submit" value="@textAction" class="btn btn-primary" />
                    </div>
                </form>
            </div>
            <div class="links">
                <a asp-action="List">Retour à la liste des choix</a>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
    }
